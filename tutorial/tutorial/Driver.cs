namespace tutorial;

public class Driver
{
    static void Main(String[] args)
    {
        MultiDimentionalArrays multiDimentionalArrays = new MultiDimentionalArrays();
        multiDimentionalArrays.InitAndDisplayMultiDimArray(1,1);
        multiDimentionalArrays.InitAndDisplayJaggedArray(2,4);
    }
}